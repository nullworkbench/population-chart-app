import styled from "styled-components";

const Title: React.FC = () => {
  return (
    <div>
      <H1>Population Chart</H1>
    </div>
  );
};

const H1 = styled.h1`
  margin-bottom: 2rem;
`;

export default Title;
